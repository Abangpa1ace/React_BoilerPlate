import React from 'react';
import PrimaryButton from './common/button/PrimaryButton';

const Example = () => {
  return (
    <div>
      <PrimaryButton theme={'green'}></PrimaryButton>
    </div>
  );
};

export default Example;
